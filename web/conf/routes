# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

# Playback commands  
GET     /next                       controllers.Application.next
GET     /play                       controllers.Application.pley
GET     /stop                       controllers.Application.stop
GET     /prev                       controllers.Application.prev
GET     /pause                      controllers.Application.pause
GET     /currentSong                controllers.Application.currentSong
GET     /eventfeed                  controllers.Application.eventFeed
GET     /connect                    controllers.Application.connect
GET     /blank                      controllers.Application.blank
# Playlist commands

# Search



# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
